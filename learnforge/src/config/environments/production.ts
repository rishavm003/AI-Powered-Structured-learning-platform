// Environment specific configurations
export const environmentConfig = {
  production: {
    apiEndpoints: {
      roadmapService: '/api/roadmap',
      quizService: '/api/quiz',
      // Add other environment-specific configurations here
  }
};